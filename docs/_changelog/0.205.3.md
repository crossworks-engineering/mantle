# 0.205.3 — 2026-07-26

## Changed

- **Brand identity gets its own Logo tab.** In Settings → Appearance's preview
  strip (between Color Palette and Typography): the logo upload as the first
  column, then the wordmark and peer-name font libraries as TALL columns —
  the whole face library at a glance instead of a letterbox. The Brand
  section leaves the sidebar (back to Mode + Color theme), and the first tab
  is labeled **Theme** (it auditions the active theme; it was never a cards
  gallery).
