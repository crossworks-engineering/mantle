# v0.157.6 — Runner queues: slice-3 plan for pre-implementation audit (docs only)

A documentation-only release. No code, schema, or behaviour changed; the runs
feature stays dark behind `MANTLE_RUNS`.

## Added

- **[docs/runs-slice-3-plan.md](../runs-slice-3-plan.md)** — the slice-3 work
  plan, written to be audited *before* implementation rather than after. Sets
  out the work packages, the acceptance gate, and the crash-test shape that
  slice 3 has to pass.

## Changed

- **[docs/adr-runs-durable-execution.md](../adr-runs-durable-execution.md)** —
  small amendments to keep the ADR consistent with the plan; the substrate
  decision itself is unchanged (runs v1 stays on pg-boss + the CAS/counter
  engine, with slice 3 as the DBOS re-evaluation gate).

## Notes

- Backfilled 2026-07-22: this version shipped with a version bump but no
  changelog entry, leaving a gap in the 0.157.x sequence.
