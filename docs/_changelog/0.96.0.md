# 0.96.0 — 2026-06-29

**Mantle as a remote MCP connector.** This tag includes v0.95.0: connect
claude.ai (or any remote MCP client) to your box over HTTP with real OAuth.

## Highlights

- **Remote MCP endpoint** (v0.95.0). A shared MCP server core with an HTTP
  transport and a full OAuth 2.1 authorization server — add your box as a
  claude.ai connector. Managed from a new **Settings → MCP** screen with an
  enable gate and rate limiting.
- **Assist everywhere it edits.** The global chat can now drive the Pages,
  Tables, and App-builder assist flows directly.
