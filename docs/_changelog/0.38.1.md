# 0.38.1 — 2026-06-23

UI wording: the `/settings/config` adopt buttons are now **Commit changes** /
**Commit all** (and matching confirm/toast copy). Behaviour is unchanged — each
still writes the manifest's version of the item into the brain, the same sync the
boot reconcile makes, and never deletes operator-added items.
