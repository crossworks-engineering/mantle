# 0.205.2 (2026-07-26)

## Fixed

- **The Brand section (logo + display fonts) moved above the Color theme
  list** on Settings → Appearance. Below it, the section sat past the fold of
  the sidebar's own scroll (behind ~40 theme rows) and read as missing.
  Order is now Mode → Brand → Color theme.
