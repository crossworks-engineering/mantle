# v0.133.1 — migration journal hotfix

Fixes v0.133.0: migration 0119 (hybrid passage search's keyword index) was not registered in the migration journal, so upgraded boxes skipped it. This release registers it (idempotent — safe over a manual apply) and adds a permanent guard test so an unjournaled migration can never ship again.
