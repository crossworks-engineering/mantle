# v0.157.7 — Runner queues: slice-3 audit outcome, implement with amendments 1–4 (docs only)

A documentation-only release recording the outcome of the pre-implementation
audit of [docs/runs-slice-3-plan.md](../runs-slice-3-plan.md). No code, schema,
or behaviour changed; the runs feature stays dark behind `MANTLE_RUNS`.

## Changed

- **[docs/runs-slice-3-plan.md](../runs-slice-3-plan.md)** — verdict:
  **implement, with amendments 1–4**. The audit's required changes are folded
  into the plan itself rather than tracked separately, so the plan stays the
  single source of truth for what slice 3 builds.
- **[docs/adr-runs-durable-execution.md](../adr-runs-durable-execution.md)** —
  updated in step with the amended plan.

## Notes

- Auditing the plan before writing the code is the deliberate pattern here: the
  same approach caught the lock-order deadlock and the `run_audit` ban gap in
  [v0.157.5](./0.157.5.md), where the audit ran against already-written code.
- Backfilled 2026-07-22: this version shipped with a version bump but no
  changelog entry, leaving a gap in the 0.157.x sequence.
