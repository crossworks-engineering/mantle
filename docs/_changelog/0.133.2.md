# v0.133.2 — migration ordering hotfix

Completes the v0.133.1 fix: the 0119 journal entry was timestamped before 0118, and the migrator only applies entries newer than the last one recorded — so upgraded boxes still skipped it. Restamped correctly; the guard test now enforces migration timestamp ordering too.
