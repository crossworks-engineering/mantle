# 0.113.2 — 2026-07-03

**One version, one place.** The little version badge next to the header
wordmark is gone — it duplicated the sidebar's "v x.y.z" changelog link,
which stays (both always showed the same build version from the root
`package.json`). The sidebar link's tooltip now carries the full build
identity (version · git sha · build date) that used to live on the badge.
