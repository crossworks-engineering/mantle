# 0.83.1 — 2026-06-28

**Delegation slugs can no longer be invented.** `invoke_agent` constrains its
`agent_slug` argument to the actual delegate list, so a model can't hallucinate
an agent name and burn a turn discovering it doesn't exist.
