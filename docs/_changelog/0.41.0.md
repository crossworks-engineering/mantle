# 0.41.0 — 2026-06-24

Every in-surface Assist panel now shows what the specialist is doing right now,
instead of a blind spinner.

## Highlights

- **Live stage feedback across all specialist Assist panels.** Pages, Tables,
  Apps, and Dev-tools panels now surface the live step — "Reading the page…",
  "Editing the table…", "Building…", "Testing the API…" — generalising the
  Apps-only reference implementation into one shared layer. `lib/assist-stage.ts`
  holds a single unified slug→label map (`specialistStageLabelForStep`, tool
  slugs are globally unique) plus `currentSpecialistStage(ownerId, agentSlug)`,
  filtered by `data->>'delegated_agent_slug'` so concurrent surfaces never
  cross-talk.

## Notes

- One shared, surface-validated route (`GET /api/assist/stage?surface=…`) and a
  `useAssistStage` hook + `<SpecialistWorking>` indicator
  (`components/specialist-working.tsx`).
- Apps was migrated onto the shared layer (dropped the app-only
  `lib/apps/assist-stage.ts`, hook, route, and `AppsmithWorking`).
- Pure UI + a read-only trace query — no change to the blocking `invokeAgent`
  run path, no agent/prompt/manifest changes. `assist-stage.test.ts` guards the
  tool-loop step-name contract.
- No schema migration in this release.
