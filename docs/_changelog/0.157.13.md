# v0.157.13 — Runner queues: slice-3 build handover for final audit (docs only)

A documentation-only release. No code, schema, or behaviour changed; the runs
feature stays dark behind `MANTLE_RUNS`.

## Added

- **[docs/runs-slice3-build-handover.md](../runs-slice3-build-handover.md)** —
  the completed slice-3 build written up for the final audit: what was built
  against the amended plan, what was deliberately left out, and the state the
  auditor should verify against.

## Notes

- The audit this handover fed into concluded in [v0.157.14](./0.157.14.md); the
  slice-3 acceptance gate (the `CRASH_TEST_SHAPE=resume` crash test) passed in
  [v0.157.12](./0.157.12.md).
- Backfilled 2026-07-22: this version shipped with a version bump but no
  changelog entry, leaving a gap in the 0.157.x sequence.
