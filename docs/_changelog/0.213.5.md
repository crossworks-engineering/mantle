# 0.213.5 — 2026-07-29

Docs-only.

## Added

- `docs/scripts.md` — a reference for every script in the repo: the dev-stack
  bring-up, worktrees, dump/restore, install/update/uninstall, diagnostics,
  the maintenance runner + seeds + backfills + evals, code generators, git
  hooks and CI, plus conventions for adding a script. Linked from the README
  docs index under Operations.
